## {{#each info.namespace}}{{this}}.{{/each}}{{info.name}}{{#if arguments}}({{#each arguments}}{{#if @index}}, {{/if}}{{@key}}{{/each}}){{/if}} - {{{info.type}}}
{{#if info.deprecated}}
**WARNING: This has been deprecated!**

{{/if}}
**{{{info.blurb}}}**
{{#if info.description}}

{{{info.description}}}

{{/if}}
{{#if examples}}
### Examples
{{#each examples}}
{{#if name}}**{{name}}**{{/if}}
    
    {{{code}}}

{{/each}}
{{/if}}
{{#if arguments}}
### Arguments
{{#each arguments}}
  * `{{{@key}}} - {{{type}}}`: {{{description}}}
{{> attributes}}
{{/each}}

{{/if}}
{{#if methods}}
### Methods
{{#each methods}}
  * `{{info.name}}({{#each arguments}}{{#if @index}}, {{/if}}{{@key}}{{/each}})`: {{{info.blurb}}}{{#if return}} - returns `{{{return.type}}}`: {{return.description}}{{/if}}
{{#each arguments}}
    * `{{@key}}`: `{{{type}}}`, {{description}}
    {{> attributes}}
{{/each}}
{{/each}}

{{/if}}
{{#if emits}}
### Emitted Events
{{#each emits}}
  * `{{@key}}`: {{description}}
{{> attributes}}
{{/each}}

{{/if}}
{{#if members}}
### Members
{{#each members}}
  * `{{info.name}} - {{info.type}}`: {{info.blurb}}
{{#if attributes}}
{{#each attributes}}
    * `{{@key}} ({{type}})` - {{description}}
{{> attributes}}
{{/each}}
{{/if}}
{{/each}}

{{/if}}
{{#if return}}
### Return

`{{return.type}}` - {{{return.description}}}

{{/if}}
{{#if todo}}
### Todo
{{#each todo}}
  * {{this}}
{{/each}}

{{/if}}
### Definition

`{{{info.filename}}}:{{info.lineNumber}}`:  
`{{{info.definition}}}`

